<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.AeonTestProject" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/main_background</item>
    </style>

    <style name="Theme.AeonTestProject" parent="Base.Theme.AeonTestProject" />
</resources>